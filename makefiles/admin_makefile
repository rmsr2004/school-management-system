CC      = gcc
OBJS    = functions/functions.o admin/udp_admin.o
PROG    = admin/admin
FLAGS   = -Wall -Wextra

all: ${PROG}

clean:
	rm -f ${OBJS} *~

${PROG}:	${OBJS}
	${CC} ${FLAGS} ${OBJS} -o $@

.c.o:
	${CC} ${FLAGS} $< -c -o $@

functions/functions.o:	functions/functions.c
functions/functions.c:	functions/functions.h
